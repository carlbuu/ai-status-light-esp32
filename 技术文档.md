# AI 工作状态外接指示灯技术文档

文档版本：4.1  
更新日期：2026-07-31  
适用平台：Windows 10/11、ESP32-C3、Codex、Cursor

## 1. 系统概览

本系统由三部分组成：

```text
Codex Hooks / Sessions ─┐
                       ├─> Windows 单实例桥接程序 ─USB Serial─> ESP32-C3 ─> 红黄绿 LED
Cursor Hooks ──────────┘              │
                                     └─ 图形控制面板 / 系统托盘
```

用户在 EXE 界面中选择 Codex 或 Cursor。选择结果是单选且持久化的，只有当前平台的事件可以改变 LED；来自另一平台的旧 Hook 消息会被桥接程序忽略。

## 2. 文件组成

| 文件 | 用途 |
| --- | --- |
| `sketch_jul16a/sketch_jul16a.ino` | ESP32-C3 固件 |
| `windows/CodexStatusBridge.cs` | 单实例、IPC、状态聚合、串口与安装逻辑 |
| `windows/IntegrationManager.cs` | Codex/Cursor 配置切换、合并、备份与清理 |
| `windows/StatusForm.cs` | WinForms 控制面板和响应式布局 |
| `windows/build.ps1` | 编译、打包和自检 |
| `portable/install.ps1` | 便携安装与卸载 |
| `CodexStatusLight-OneClick.exe` | 单文件图形版 |
| `CodexStatusLight-portable.zip` | 便携安装包 |

程序使用 .NET Framework 自带编译器构建，不要求目标电脑安装 .NET SDK。

## 3. LED 与串口协议

### 3.1 引脚

- 红灯：GPIO2
- 黄灯：GPIO3
- 绿灯：GPIO4
- 三路均为高电平点亮
- 三路使用 2 kHz、8 位 LEDC 硬件 PWM，亮度范围为 5%～100%

### 3.2 串口

- 115200 bit/s
- 8N1
- ASCII 文本，一行一条命令

| 命令 | 含义 |
| --- | --- |
| `IDENTIFY` | 设备识别 |
| `PING` | 刷新心跳 |
| `THINKING` / `WORKING` / `WORKING1` | 1 个任务，黄灯常亮 |
| `WORKING2` | 2 个任务，黄灯双闪 |
| `WORKING3` / `WORKING3PLUS` | 3 个及以上任务，黄灯三闪 |
| `PERMISSION` / `WAITING` | 绿灯闪烁 |
| `COMPLETE` / `IDLE` | 绿灯常亮 |
| `ERROR` | 红灯常亮 |
| `OFF` | 全部熄灭 |
| `SUSPEND` | 全部熄灭，并暂停心跳超时直到收到下一个状态命令 |
| `BRIGHTNESS 5-100` | 设置三路 LED 的 PWM 亮度百分比 |
| `BRIGHTNESS?` | 查询当前固件亮度 |

新版设备签名为 `CODEX_STATUS_LIGHT:5`。桥接程序仍能识别版本 1/2/3/4；
版本 1/2 不支持双闪和三闪，会自动降级为单任务黄灯常亮，版本 1/2/3
均不支持亮度命令，版本 4 不支持 `SUSPEND`。Windows 程序连接版本 4/5 设备后会发送已保存的亮度，仅对版本 5 发送休眠命令。固件连续约
7 秒未收到有效命令时进入红灯错误状态；`SUSPEND` 状态不参与心跳超时。

## 4. Windows 桥接程序

### 4.1 单实例与 IPC

- 互斥量：`Local\CodexStatusLightBridge`
- UDP：`127.0.0.1:38451`
- Hook 客户端只负责把事件发送给常驻实例，不直接访问串口。
- 再次打开 EXE 会显示已有实例的窗口。

### 4.2 状态优先级

多个任务或会话并存时，程序按会话统计仍在运行或等待权限的任务。Hook
和 Sessions 同时提供信息时取两者计数的较大值，避免同一个任务被重复计算。
状态优先级为：

```text
等待允许 > 工作中 > 报错 > 完成未查看 > 已查看/空闲
```

“按运行任务数量闪烁黄灯”默认关闭，此时任意数量的运行任务都发送 `THINKING`。
开启后，工作状态根据数量发送不同命令：

```text
1 个任务 -> THINKING
2 个任务 -> WORKING2
3 个及以上 -> WORKING3
```

权限状态使用绿灯闪烁；没有运行任务但存在完成未查看项目时使用绿灯常亮；
完成项目均已查看且没有运行任务时发送 `OFF` 自动熄灯。
Cursor 完成事件会额外产生约 3 秒的绿灯提示；如果对应项目仍未读，绿灯会继续常亮。

状态按会话保存；超时的记录会清理。手动“关闭显示”优先于 AI 状态。

### 4.3 待检查项目

- Codex：读取 `%USERPROFILE%\.codex\.codex-global-state.json` 中的
  `unread-thread-ids-by-host-v1`，该列表与侧边栏项目黄点同步。
- Cursor：以只读方式查询 `%APPDATA%\Cursor\User\globalStorage\state.vscdb`，
  使用 `composerHeaders` 中的 `hasUnreadMessages`，并兼容旧版的合并 JSON 存储。
- Cursor Hook 输入损坏时只恢复会话、任务和事件元数据；无法恢复或缺少会话标识时忽略该事件，
  不创建 `unknown` 运行任务。
- 每秒轮询一次。读取失败时保留上一次状态，避免数据库短暂锁定导致误熄灯。
- “全部标记已检查”仅把当前未读 ID 写入本程序的确认列表，不修改 Codex/Cursor 数据；
  平台的未读标记消失后会自动清理对应确认记录。

### 4.4 串口发现

自动模式优先尝试 COM15，再按端口名扫描其他端口。连接前发送 `IDENTIFY`，只接受返回本项目签名的设备。连接后每 2 秒发送一次 `PING`。

桥接程序监听 Windows 电源事件：休眠前发送 `SUSPEND`；唤醒后若串口仍打开则立即重发当前完整状态，否则立即重新扫描设备。休眠期间停止状态轮询和心跳发送。

### 4.5 退出

退出路径不会等待串口锁。即使另一个线程正在执行串口操作，托盘“退出”和界面“退出程序”也会快速关闭；后台线程随后由进程终止统一释放。

## 5. 平台配置

### 5.1 持久化

当前平台保存于：

```text
%LOCALAPPDATA%\CodexStatusLight\settings.json
```

合法值为 `Codex` 或 `Cursor`，缺失或无效时默认 `Codex`。
同一文件还保存 `Brightness` 和 `TaskCountBlinkEnabled`。亮度有效范围为 5～100，
缺失或无效时默认 100；任务数量闪烁开关缺失时默认关闭。

### 5.2 切换算法

点击“应用并配置”后：

1. 从 `%USERPROFILE%\.codex\hooks.json` 移除属于本程序的 Hook。
2. 从 `%USERPROFILE%\.cursor\hooks.json` 移除属于本程序的 Hook。
3. 只向新选择的平台添加 Hook。
4. 保留所有不属于本程序的 Hook。
5. 修改前生成 `.backup-yyyyMMdd-HHmmss-fff` 备份。
6. 原子写入配置和平台设置。
7. 清空旧平台的运行时会话状态。

桥接程序还会检查消息来源，忽略非当前平台的消息。

### 5.3 Codex

用户配置路径：

```text
%USERPROFILE%\.codex\hooks.json
```

| 事件 | 状态 |
| --- | --- |
| `UserPromptSubmit` | 工作中 |
| `PermissionRequest` | 等待允许 |
| `PostToolUse` | 工作中 |
| `Stop` | 已完成 |

Codex 模式还会增量监测 `%USERPROFILE%\.codex\sessions` 中最近的 JSONL 文件，补充桌面版可能未触发的开始、完成和授权状态。Cursor 模式会关闭这项轮询，避免 Codex 误控灯光。

### 5.4 Cursor

用户配置路径：

```text
%USERPROFILE%\.cursor\hooks.json
```

使用的事件：

- `sessionStart`、`sessionEnd`
- `beforeSubmitPrompt`
- `preToolUse`、`postToolUse`、`postToolUseFailure`
- `beforeShellExecution`、`afterShellExecution`
- `beforeMCPExecution`、`afterMCPExecution`
- `stop`

事件通过标准输入传入 JSON。Windows 版 Cursor 3.13 使用 UTF-8 临时文件和
PowerShell 管道传输输入；桥接程序直接读取标准输入字节，识别 UTF-8 BOM
及 UTF-16 BOM，避免系统代码页把会话标识解析成未知任务。

权限 Hook 由 PowerShell 外层直接向标准输出写入 JSON，不依赖 WinForms
图形 EXE 的控制台输出句柄。

状态映射：

- 提交提示词：重置会话并进入工作中
- 一般工具事件：工作中
- Shell/MCP 执行前：等待允许，并返回 `{"permission":"ask"}`
- `WebSearch` / `WebFetch` 工具执行前：等待允许，并返回 `{"permission":"ask"}`
- Shell/MCP 执行后：工作中
- `stop` 正常完成：已完成
- `stop` 错误或中止：报错
- `sessionEnd` 出错：报错；否则清理会话

Cursor 没有覆盖所有原生权限弹窗的被动事件。为了使绿色闪烁与等待动作一致，
本程序主动让 Shell、MCP、网页搜索和网页读取操作进入 `ask`。切换回 Codex
后会移除这些 Hook。

官方参考：<https://cursor.com/cn/docs/hooks>

## 6. 图形界面

控制面板提供：

- Codex/Cursor 单选
- 一键安装和配置
- 开机启动开关
- 自动或手动串口选择
- 连接、断开和重新扫描
- 实时设备、固件、AI 与显示状态
- 五种灯光测试
- 5%～100% 实时亮度滑块（180 ms 防抖并自动保存）
- “按运行任务数量闪烁黄灯”开关（即时保存并生效）
- 待检查项目数量和“全部标记已检查”备用按钮
- 托盘常驻与快速退出

界面按窗口宽度缩放字体、按钮和间距；宽度不足时状态区从四列变为两列，设置区从两列变为上下排列；高度不足时显示滚动条，避免底部按钮被裁掉。

## 7. 安装、启动与卸载

安装目录：

```text
%LOCALAPPDATA%\CodexStatusLight\CodexStatusBridge.exe
```

开机启动使用当前用户注册表：

```text
HKCU\Software\Microsoft\Windows\CurrentVersion\Run
CodexStatusLightBridge="<安装路径>\CodexStatusBridge.exe" --background
```

关闭开机启动时会同时清理 32/64 位注册表视图中的历史名称，以及旧版本可能创建的启动文件夹快捷方式、CMD 或 BAT 文件。

便携卸载先调用 EXE 的 `--remove-integrations` 和 `--uninstall-startup`，再停止已安装实例并删除 EXE。因此 Codex 与 Cursor 中属于本程序的 Hook 都会被清理。

## 8. 命令行接口

| 参数 | 用途 |
| --- | --- |
| `hook <STATE>` | Codex Hook 客户端 |
| `cursor-hook` | Cursor 普通事件 Hook |
| `cursor-permission-hook` | Cursor 权限事件 Hook |
| `--configure-platform Codex` | 配置 Codex |
| `--configure-platform Cursor` | 配置 Cursor |
| `--remove-integrations` | 清理两个平台中的本程序 Hook |
| `--install-startup` | 启用当前用户开机启动 |
| `--uninstall-startup` | 取消并清理开机启动 |
| `--display-off` | 保持串口心跳并关闭全部 LED |
| `--background` | 后台启动 |

## 9. 构建与自动测试

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\windows\build.ps1
```

构建输出：

```text
windows\publish\CodexStatusBridge.exe
CodexStatusLight-OneClick.exe
CodexStatusLight-portable.zip
```

自检覆盖：

- LED 状态聚合及错误优先级
- Codex 与 Cursor 事件映射
- Windows UTF-8/BOM Hook 输入解析
- Cursor 图形 EXE 权限 JSON 外层输出
- JSON Hook 合并、备份和无关配置保留
- Codex→Cursor→Codex 切换
- 窗口响应式参数
- 亮度和任务数量闪烁设置的持久化、平台切换保留及旧固件兼容
- Codex/Cursor 未读项目解析、确认过滤和自动熄灯映射
- 串口锁占用时的非阻塞退出

只有自检输出以 `PASS` 开头时构建才算成功。

## 10. 验收清单

1. EXE 可在界面选择 Codex 或 Cursor，且只能单选。
2. 应用后只有所选平台的配置包含本程序 Hook。
3. 两个平台原有的无关 Hook 均保留。
4. 关闭任务数量闪烁时，1、2、3 个并行任务均显示黄灯常亮；开启后分别显示常亮、双闪和三闪。
5. 权限请求显示绿灯闪烁；完成未查看显示绿灯常亮；全部已查看且无运行任务时自动熄灯。
6. 切换平台后重启对应应用，任务计数与状态映射正确。
7. 放大窗口后文字和控件同步放大。
8. 窄窗口自动换行，高度不足可滚动到底部设置。
9. 取消开机启动后，注册表和旧启动文件均无本程序项目。
10. 托盘退出在串口被占用时仍快速完成。
11. 根目录 EXE 与 `windows\publish` EXE 的 SHA-256 一致。
12. ZIP 内包含可直接运行的 EXE、安装脚本和卸载脚本。
13. 固件 4 可在 5%～100% 间调光；重连和切换平台后亮度保持，关闭显示仍完全熄灭。
14. 多个黄点项目只点击其中一部分时绿灯保持；点击最后一个且没有运行任务时自动熄灯。
